# ContextAI: PM Assistant Bot

## 💡 Проблема
Критически важная информация (решения, задачи, статусы) часто теряется в больших объемах переписки в рабочих чатах, или остается в неструктурированном виде после созвонов. Product Manager'ам приходится тратить часы на вычитку "стен текста" для актуализации статусов.

## 🚀 Решение
**ContextAI** — это Telegram-бот-ассистент, использующий LLM (Large Language Model) для автоматического извлечения структурированных данных из неструктурированного текста.

**Функционал MVP:**
1.  **Парсинг чатов:** Преобразование копипаста из групповых чатов (Telegram) в четкий список задач.
2.  **Анализ заметок:** Структурирование сбивчивых заметок со встреч или устных обсуждений.

## 🔑 Ключевая ценность (Product Value)
* **Экономия времени:** Сокращает время на актуализацию статусов для всей команды.
* **Снижение рисков:** Минимизирует риск потери критически важных "Action Items".
* **Демонстрация навыков:** Кейс, демонстрирующий умение быстро создавать работающие AI-инструменты.

## 🛠️ Технический стек
* **Язык:** Python 3.10+
* **Бот-фреймворк:** `aiogram`
* **Модель LLM:** OpenAI `gpt-4o-mini` (выбрана за оптимальное соотношение цены/качества).
* **Секреты:** `python-dotenv` (для безопасного хранения ключей).

## Как начать
1.  Клонируйте репозиторий.
2.  Установите зависимости (`pip install -r requirements.txt`).
3.  Создайте файл `.env` с вашими `BOT_TOKEN` и `OPENAI_API_KEY`.
4.  Запустите `python main.py`.

### Использование
Отправьте боту в личные сообщения небольшой объем текста/перешли сообщение. Бот вернет структурированный ответ.